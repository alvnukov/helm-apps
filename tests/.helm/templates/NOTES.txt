{{- $env := "unknown" -}}
{{- if and (hasKey .Values "global") (hasKey .Values.global "env") -}}
{{- $env = toString .Values.global.env -}}
{{- end -}}
Helm Apps chart installed successfully.

Release:
  Name: {{ .Release.Name }}
  Namespace: {{ .Release.Namespace }}
  Environment: {{ $env }}

Useful Helm commands:
  helm status {{ .Release.Name }} -n {{ .Release.Namespace }}
  helm get values {{ .Release.Name }} -n {{ .Release.Namespace }}
  helm get manifest {{ .Release.Name }} -n {{ .Release.Namespace }}

Useful Kubernetes checks:
  kubectl get deploy,statefulset,job,cronjob -n {{ .Release.Namespace }}
  kubectl get svc,ingress -n {{ .Release.Namespace }}
  kubectl get pods -n {{ .Release.Namespace }}

If rollout is stuck:
  kubectl describe pods -n {{ .Release.Namespace }}
  kubectl logs -n {{ .Release.Namespace }} deploy/<app-name> --tail=200
